栈桢拥有“现进后出”的原理，从高地址向低地址蔓延存入。

ss：栈底寄存器
sp：栈顶指针
bp:当前函数的基地址
ip：指令寄存器

从栈底到栈顶：
1.调用函数参数（到1）
2.下一指令地址
3.调用函数的基地址
4.函数局部变量（1始）

一般形式：
push bp
mov bp,sp
......
pop bp
retn

最后ip执行返回指令，而这里是栈桢主要可以被shellcode修改的地方。
